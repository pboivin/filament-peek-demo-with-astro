---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact">
    <main>
        <div class="relative min-h-[200px] flex items-center justify-center bg-gray-100">
            <div class="relative z-1 p-2 text-4xl text-gray-700 lg:text-6xl">
                <h1>Contact Us</h1>
            </div>
        </div>

        <div class="max-w-wide mx-auto p-2">
            <div
                class="max-w-[600px] mt-8 mx-auto"
                x-data="{
                    isSent: false,
                    values: {},
                    errors: {},

                    submit() {
                        this.errors = {};

                        if (Number(this.values.quiz) !== (8 - 1)) {
                            this.errors.quiz = true;
                        } else {
                            // Just an example, here you would send the contact form data to the appropriate service...

                            this.isSent = true;
                        }
                    },
                }"
            >
                <form
                    x-on:submit.prevent="submit"
                    x-show="!isSent"
                >
                    <div class="mt-4">
                        <div>
                            <label for="name_input" class="block text-sm font-medium text-gray-700">Name</label>

                            <input
                                type="text"
                                name="name"
                                id="name_input"
                                class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-700 focus:ring-green-700 sm:text-sm"
                                required="required"
                                x-model:value="values.name"
                            />
                        </div>
                    </div>

                    <div class="mt-4">
                        <div>
                            <label for="email_input" class="block text-sm font-medium text-gray-700">Email</label>

                            <input
                                type="email"
                                name="email"
                                id="email_input"
                                class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-700 focus:ring-green-700 sm:text-sm"
                                required="required"
                                x-model:value="values.email"
                            />
                        </div>
                    </div>

                    <div class="mt-4">
                        <div>
                            <label for="message_input" class="block text-sm font-medium text-gray-700">Message</label>

                            <textarea
                                name="message"
                                id="message_input"
                                class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-700 focus:ring-green-700 sm:text-sm"
                                rows="8"
                                required="required"
                                x-model:value="values.message"></textarea>
                        </div>
                    </div>

                    <div class="mt-4">
                        <div>
                            <label for="quiz_input" class="block text-sm font-medium text-gray-700">What is 8-1?</label>

                            <input
                                type="text"
                                name="quiz"
                                id="quiz_input"
                                class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-700 focus:ring-green-700 sm:text-sm"
                                required="required"
                                x-bind:class="{ 'border-red-500': errors.quiz }"
                                x-model:value="values.quiz"
                            />
                        </div>
                    </div>

                    <div class="mt-4">
                        <button
                            type="submit"
                            class="inline-flex justify-center rounded-md border border-transparent bg-green-700 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-700 focus:ring-offset-2"
                        >
                            Send
                        </button>
                    </div>
                </form>

                <div
                    class="text-center"
                    x-text="isSent ? 'Thank you, we’ll be in touch!' : ''"
                ></div>
            </div>
        </div>
    </main>
</Layout>
